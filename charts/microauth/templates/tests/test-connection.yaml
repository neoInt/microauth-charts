apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "microauth.fullname" . }}-test-connection"
  labels:
    {{- include "microauth.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "microauth.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
